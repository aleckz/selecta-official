<div ng-controller="SongSearchController">
  <div class="backgroundimageStyling">
  <div class="navbar navbar-default navbar-fixed-top">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Selecta</a>
        </div>
        <form class="navbar-form navbar-left" role="search">
          <div class="form-group">
            <input type="text" ng-model="searchTerm" name="searchstring" class="form-control" placeholder="Start discovering...">
          </div>
          <button ng-click="doSearch()" type="submit" class="btn btn-default">Submit</button>
        </form>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav">
            <li><a href="#about">Like History</a></li>
            <li><a href="#about">Sign up</a></li>
            <li><a href="#about">Log in</a></li>
          </ul>
        </div>
      </div>
    </div>
    <ul ng-repeat="song in songs">
      <li>
        <a ng-href="#/track/{{song.id}}" ng-click="clearList()">{{song.title}}</a>
      </li>
    </ul>
  </div>
</div>

<div>
   <ui-view></ui-view>
</div>

<div id='test'>Cock sucker mother fucker</div>
